# SwirlRead — Develop Docs Index

> Status: Implementation in progress · Last updated: 2026-05-03

The product is fully designed. These documents tell you HOW to build it.

## Read in This Order

| #   | File                                                                 | What it gives you                                                                        |
| --- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 1   | [architecture-overview.md](./architecture-overview.md)               | Module map, layer boundaries, the `VaultFileSystem` interface, render pipeline structure |
| 2   | [tech-stack.md](./tech-stack.md)                                     | Concrete library choices with versions and bundle budget                                 |
| 3   | [engineering-principles.md](./engineering-principles.md)             | Code style, naming, architecture rules, testing philosophy, definition of done           |
| 4   | [reader-experience-craft-plan.md](./reader-experience-craft-plan.md) | Reader UX craft plan: document header, chrome, navigation, context rail, command palette |
| 5   | [lightweight-editing-plan.md](./lightweight-editing-plan.md)         | Phase 2 quick-edit design: write interface, permission escalation, state, UI, save loop  |
| 6   | [phase-1-implementation-plan.md](./phase-1-implementation-plan.md)   | **The ordered task list.** Execute these tasks in milestone order.                       |
| 7   | [work-log.md](./work-log.md)                                         | Reverse-chronological implementation log. Update on every task completion.               |

## Current Status

- ✅ **M0.1** — Project bootstrap (Vite + React 19 + TS strict)
- ✅ **M0.2** — Tailwind v4 + brand tokens + 4-theme architecture
- ✅ **M0.3** — ESLint 9 + Prettier + Vitest + RTL; unified `pnpm check` pipeline
- ✅ **M0.4** — React Router v7 scaffold; 4 placeholder routes; 8 tests passing
- ✅ **M0.5** — Self-hosted fonts (Source Serif 4, Inter, JetBrains Mono) via @fontsource
- ✅ **Milestone 0 complete** — project bootstrap done
- ✅ **M1.1** — `VaultFileSystem` interface + path utilities (34 unit tests)
- ✅ **M1.2** — `FSAPIVaultAdapter` (32 unit tests with FSAPI mock)
- ✅ **M1.3** — Folder picker UI + session vault registry; pick→register→navigate flow works end-to-end
- ✅ **M1.4** — Zustand `useVaultStore` + Dexie persistence; registry shim deleted
- ✅ **M1.5** — Markdown rendering pipeline (CommonMark + GFM + frontmatter + sanitization)
- ✅ **M1.6** — DocumentPage renders real Markdown end-to-end (126 tests passing)
- ✅ **Milestone 1 complete** — first real render from a real vault works
- ✅ **M3.2 + M3.3** — Wikilinks: parse, resolve, click-to-navigate
- ✅ **M3.5 + M3.6** — Callouts: 14 canonical types + 12 aliases, themed colors, Lucide icons
- ✅ **M3.12** — Shiki code highlighting (dual-theme, 27 langs bundled, async pipeline)
- ✅ **M2.3** — UI store + theme switcher (themes finally reachable via header dropdown, persisted)
- ✅ **M3.7 + M3.8** — Embeds (`![[file]]`): images, video, audio, recursive markdown with cycle + depth guards
- ✅ **M3.9** — Highlights `==text==` → `<mark>` with theme-tuned tints
- ✅ **M3.4** — Wikilink hover preview (Floating UI; 400 ms delay; safePolygon grace; cancel-safe fetch)
- ✅ **M4.1** — Vault home detection (`index.md` / `home.md` / `README.md` auto-redirect); navigable directory listing with breadcrumbs at vault root + every sub-folder
- ✅ **M4.3** — Persistent file-tree sidebar (lazy expand, ancestor auto-expand, active-file highlight, toggle button in AppShell, persisted preference) (266 tests passing)
- ✅ **M4.7** — Recent files store (last 20 per vault, persisted in IndexedDB, sidebar top section, command-palette ready) (276 tests passing)
- ✅ **M2.4** — Settings panel UI (Radix right drawer; theme, typography, content width, file tree toggle, reset) (281 tests passing)
- ✅ **M4.4** — Backlinks index (resolved wikilink edges, incremental per-file indexing, IndexedDB cache, M4.5-ready) (290 tests passing)
- ✅ **M4.5** — Backlinks panel UI (document-bottom source links, context snippets, navigation) (293 tests passing)
- ✅ **M3.10** — Frontmatter display (YAML/TOML extractor + metadata/raw/hidden modes + settings panel toggle) (331 tests passing)
- ✅ **M3.13** — Mermaid diagrams (lazy-loaded renderer; theme-aware; safe fallback to source on failure) (347 tests passing)
- ✅ **M2.7** — Scroll position memory (per-file `scrollY` persisted in IndexedDB, rAF-restored after render, debounced save, capped at 500 per vault)
- ✅ **M4.6** — Table of contents (right-rail; DOM-based heading extraction with Unicode slugs; IntersectionObserver scroll-spy; lazy-loaded chunk; toggle in shell + settings) (381 tests passing)
- ✅ **M4.2** — Section detection (top-level dirs auto-detect `*-map.md` / `<dirname>.md` / index/home/README homes; section rows in file tree become section-home links with chevron-only expand) (397 tests passing)
- ✅ **M5.1** — Command palette shell (cmdk + Radix Dialog; ⌘K / Ctrl+K global binding with editable-target guard; recents-only mode; lazy chunk; ⌘K trigger button in header) (415 tests passing)
- ✅ **M5.2** — Fuzzy file-name search (BFS vault walker + per-vault cache; cmdk-scored Files group keyed off URL vault; CJK paths supported; auto-fetch only when palette opens) (426 tests passing)
- ✅ **M3.14** — Tags (`#tag`/`#nested/tag` body syntax, Unicode-aware lookbehind regex skipping URLs/inline-code/links; tag indexer combines body + frontmatter `tags:`; clickable `<Tag>` opens lazy Radix-backed `TagsPanel` listing every file with that tag) (468 tests passing)
- ✅ **M2.6** — F-key zen mode (global hotkey with editable-target guard, Esc-exit, body-class application via existing `useApplyUIPrefs`, hides header + both sidebars while keeping reading content centered) (476 tests passing)
- ✅ **M3.1** — GFM polish (scrollable tables w/ alt-row tint + alignment-aware cells, themed task-list checkboxes, distinct strikethrough, footnotes section with backref styling and `:target` highlight) (487 tests passing)
- ✅ **M9.5** — Route error boundaries (`ErrorFallback` mounted at `/`, `/app`, and `:vaultId` levels — parent layout chrome stays mounted while a child crash shows graceful fallback w/ stack details) (492 tests passing)
- ✅ **M3.11** — KaTeX math (lazy) (`remark-math` + `remark-math-shim` emit custom `<math-inline>` / `<math-block>` elements; `MathInline`/`MathBlock` wrappers dynamic-import `MathRenderer` which dynamic-imports KaTeX runtime → 77 KB stays in its own chunk; KaTeX CSS eager (8 KB gz) to avoid flash-of-unstyled-math; safe fallback to source on parse error) (499 tests passing)
- ✅ **M6.3** — Auto-restore (boot pass `autoRestoreVaults` re-attaches FSAPI adapters whose browser permission still holds; `ReauthorizeVault` component prompts for one-click re-grant from a user gesture when permission has lapsed; existing handle persistence promoted to first-class) + **M6.1** (`VaultSwitcher` dropdown in header — registered vaults list + active marker + "Open another vault" CTA) + **M6.4** (LandingPage shows recent-vaults list with relative dates for returning users, hides fresh-user CTAs) (515 tests passing)
- 🟡 **M6.2** — De-facto done. All per-document state is already vault-keyed (reader-store recents/scroll, backlinks, tag index, walked-files cache, file-tree listings). UI prefs intentionally remain global; per-vault prefs deferred as opt-in.
- ✅ **Audit fix B1 + cleanup fan-out** — `removeVault` now calls `adapter.dispose()` (revokes blob URLs), wipes per-vault rows from `recentFiles`/`scrollPositions`/`backlinks`/`handle-storage`, and invalidates every in-memory cache (file-tree listings, tag index, walked files, backlinks, reader-store recents+scrolls). New `forgetVault()` on reader-store + `invalidate*` helpers on the file-tree and backlinks caches. (520 tests passing)
- ✅ **M9.4** — Keyboard shortcuts help overlay (`?` opens; lazy Radix Dialog; single source of truth for every binding; respects editable-target guard) (531 tests passing)
- ✅ **M5.4 + M5.5** — Full-text search mode + multi-mode prefix routing (`>` prefix opens content search via MiniSearch; CJK-aware tokenizer using `Intl.Segmenter`; per-vault promise-cached index; lazy chunk so MiniSearch ships only on first `>` use; main bundle actually shrunk by 0.30 KB after dynamic-import refactor) (545 tests passing)
- 🟡 **M5.3** — Web Worker upgrade for the index build deferred as a transparent perf optimization. In-thread build is sub-frame for the 5_000-file walker cap; worker swap can land later behind the same `getFullTextIndex` API.
- ✅ **RX1 Document Page Craft + RX7 Productized States (DocumentPage scope)** — page header now derives an article-style title (frontmatter `title` → first body H1 → cleaned filename). Vault id + path moved into a quiet monospace breadcrumb. FrontmatterPanel `metadata` mode no longer repeats the title (page header owns it). `<DocumentSkeleton>` replaces "Reading…" with a stable column-width placeholder; missing-file and error states reworded as calm two-line cards. (569 tests passing)
- ✅ **RX3 Knowledge Navigation Sidebar** — left rail reorganized into `Continue` (latest file with saved scroll position, "Resume" tag) → `Recent` (next 4) → `Sections` (top-level dirs with detected `*-map.md` homes, alphabetical) → `Files` (full tree, unchanged). Continue/Recent dedupe so the resume entry never repeats below. Sections block hides when no top-level dir has a home. (574 tests passing)
- ✅ **RX4 Context Rail** — right rail gains a compact context section above the TOC: page tags (clickable chips, capped at 6 with `+N` overflow), backlinks count, outgoing-links count. Each chip self-hides when empty. The whole rail collapses to nothing when there are no headings AND no context (no more noisy "No headings."). DocumentPage publishes tags + outgoing-link count to `useTocStore.context` after every render; backlinks count fetched inside the rail via the existing per-vault cache. (579 tests passing)
- ✅ **RX6 Command Palette navigation polish** — palette gains two new groups in non-search modes: `Headings (this document)` jumps to anchors via `requestAnimationFrame` + smooth scroll without leaving the keyboard; `Sections in <vault>` lists top-level dirs with detected homes. Headings only render when `useTocStore.context.path` matches the URL path (so old headings don't haunt a new doc). cmdk's filter ranks across all groups when typing — "intro" can find a heading, file, or section equally well. (583 tests passing)
- ✅ **RX5 Backlinks As Reading Continuation** — pure `rankBacklinks(list, { recentSourcePaths, currentPath })` reorders by recency-of-source-document, then same-top-level-section affinity, then alphabetical fallback. `BacklinksPanel` reads recents from `useReaderStore` and passes them in (current document filtered out). The `[[wikilink]]` reference inside each snippet is now wrapped in a `<mark>` so the eye lands on the connection. The whole panel returns `null` when the ready-state list is empty — no more "No backlinks yet." status row competing with the document's actual ending. (589 tests passing)
- ✅ **M7.1 + M7.2 + M7.7 + M7.8 — Universal File Reader (first slice)** — pure `getRendererKind(path)` dispatcher routes every non-Markdown vault file to one of `text` / `code` / `binary`. `PlainTextRenderer` shows monospace pre with theme-aware code-bg. `CodeFileRenderer` wraps the source in a CommonMark fence (width auto-grown past any internal backtick run) and reuses the existing Markdown pipeline so Shiki dual-theme highlighting works for free across 30+ extensions. `UnsupportedRenderer` shows a calm card (file name, ext, size, modified date, path) for binaries / archives / fonts / PDFs — readText is never called on those bytes anymore. DocumentPage `LoadState` gained `text` / `code` / `binary` variants; the old `isMd` field was removed. (607 tests passing)
- ✅ **M7.6 — Media renderer** — dispatcher upgraded to a discriminated union with a `media` kind (subtypes: `image` / `video` / `audio`). 23 media extensions (including `.svg`, treated as image so opening a logo shows the picture, not the markup) now route to a new `MediaRenderer` that loads `vault.getBlobURL()` straight into a native `<img>` / `<video controls>` / `<audio controls>`, with a figcaption showing file name + size. Shared `useBlobURL` hook extracted from `EmbedNode` so inline `![[file]]` embeds and page-level media stay in lockstep. Theme-aware framing across Sepia/Light/Dark/OLED. (612 tests passing)
- ✅ **M7.3 — CSV / TSV table renderer** — hand-rolled RFC 4180-compatible `parseDelimited` (handles quoted fields, escaped `""`, CRLF, newlines inside quoted cells, CJK, configurable delimiter, `maxRows` early-stop). New `table` kind in the dispatcher carries the right delimiter for `.csv` / `.tsv` / `.tab`. `CsvRenderer` shows the first row as `<thead>`, alternating-row tint via `nth-child`, sticky header inside an overflow-x scroll region, tabular-nums for the cells. Body rows capped at 1000 with a "Show all" button + a row/column counter that updates honestly. No new dependencies; ~80 LOC total parser + renderer. (628 tests passing)
- ✅ **M7.5 — HTML sandboxed renderer** — `.html` / `.htm` get their own dispatcher kind (no longer routed to code). `HtmlRenderer` renders into an iframe with `sandbox=""` (the most restrictive value the platform ships: no scripts, no top-nav, no forms, no plugins, opaque origin via `srcDoc`) plus a "Sandboxed" badge so the reader knows scripts are off, plus a Preview ↔ Source toggle that flips into the existing `CodeFileRenderer` for syntax-highlighted markup. Zero new dependencies — the browser sandbox is the security layer; DOMPurify would be redundant weight. (633 tests passing)
- ✅ **M7.4 — JSON tree renderer** — `.json` / `.jsonc` get a foldable tree by default. `JSON.parse` after a `stripJsonComments` pass for JSONC; failure auto-pins to source view + surfaces the parse error. Each collection node (object/array) owns its own `useState` collapse flag and starts open at depth < 2 (root + first level), collapsed below — so a 1000-key blob doesn't paint as a wall. Tree ↔ Source toggle reuses the HtmlRenderer toolbar pattern. **Milestone 7 is now feature-complete for v0.1** (only deferred polish: image lightbox, audio control theming). (645 tests passing)
- ✅ **M9.1 — Perf pass (first slice)** — main bundle 262.91 KB gz → **243.38 KB gz** (-19.53 KB; **first time below the 250 KB target**). Two lazy splits: (1) all six M7 page-level renderers (CodeFile / Csv / Html / Json / Media / Unsupported) wrapped in `React.lazy` + `Suspense fallback={null}` — Markdown readers no longer pay for them; (2) `WikilinkPreview` (Floating UI runtime ~17 KB gz) only dynamic-imports the first time the user hovers / focuses a resolved wikilink. Module-level promise cache means subsequent links reuse the loaded module synchronously. Reader who never hovers a link never downloads the popover machinery. (645 tests passing, no behaviour regressions)
- ✅ **M8.1 + M8.2 + M8.3 — Sample vault, end-to-end** — `SampleVaultAdapter` is a clean in-memory `VaultFileSystem` (path→bytes map, lazy blob URLs with `dispose()` revoke, idempotent permission API). Seven hand-written Markdown notes ("Reading in the Age of AI") interlinked through wikilinks, exercising callouts, code, math, mermaid, tags, highlights, embeds, and an SVG asset. Landing page's "Try with sample vault" CTA now registers the adapter and routes to `/app/sample-reading-in-the-age-of-ai` so a fresh user sees SwirlRead render in two clicks, before granting any disk permission. (654 tests passing, main bundle 247.96 KB gz — still inside the 250 budget)
- ✅ **M9.5 — DocumentPage error UX strengthened** — error state now offers a "Try again" button (driven by a `retryToken` that re-runs the load effect) plus a collapsible `<details>` block exposing the raw error message. Reads as "this is a SwirlRead problem, your content is fine"; styled in line with the rest of the empty/error block.
- ✅ **M9.3 — 思源宋体 self-hosted** — `@fontsource/noto-serif-sc` (chinese-simplified-400 subset) added to the import list and threaded into `--font-serif` after `Source Serif 4`. Browser only fetches the 1.5 MB woff2 when a document actually contains a CJK glyph that the Latin face can't render; pure-English pages pay nothing. `--font-serif` updated from the placeholder `Source Han Serif SC` to the real `Noto Serif SC`. Traditional Chinese still falls through to system 'Songti SC' / OS Han Serif at no bundle cost.
- ✅ **M9.2 — Responsive layout** — file-tree sidebar lifts out of flow into a fixed-position drawer below 1024 px (drawer + translucent backdrop click-to-dismiss; React markup is identical, pure CSS controls the drawer behaviour). Settings panel goes full-width below 768 px so its controls aren't cramped on tight tablets. TOC hide-below-1100 px from M4.6 stays in place. iOS Safari is still a known-broken target for v1 (no FSAPI). (654 tests passing, main bundle 248.00 KB gz, CSS 21.96 KB gz)
- ✅ **M9.6 — Open-source surface** — root `README.md` for newcomers (positioning, status, quickstart, tech stack, project layout), `LICENSE` (MIT, the long-promised one), `CONTRIBUTING.md` (the product principles + engineering style + PR flow), `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1). Repo now reads as a serious open-source project from the GitHub landing page.
- ✅ **M9.7 — CI workflow** — `.github/workflows/ci.yml` runs typecheck → lint (max-warnings 0) → format:check → test → build on every PR and on every push to main. pnpm 10 + Node from `.nvmrc` (22), with concurrency cancellation so rapid PR iterations don't queue up redundant runs. PR template at `.github/pull_request_template.md` reminds contributors to update `docs/develop/work-log.md`. Vercel deploy hook is M9.8's job (needs the production token).
- 🟡 **M9.8 — Launch readiness (code-side done, ops-side pending)** — `vercel.json` (SPA rewrites, immutable cache for hashed assets, sane security headers), `.github/workflows/deploy.yml` (push-to-main → typecheck/lint/format/test → `vercel build --prod` → `vercel deploy --prebuilt --prod`, with `VERCEL_TOKEN` / `VERCEL_ORG_ID` / `VERCEL_PROJECT_ID` secrets documented inline). Launch docs in `docs/launch/`: `launch-checklist.md` (pre-flight + day-of + post-launch boxes), `show-hn.md` (HN post draft, deliberately unhyped), `announcement.md` (Twitter / Mastodon / r/ObsidianMD / Lobste.rs variants), `README.md` orienting the launch dir. **Code is launch-ready.** Remaining is operational: register domain, link Vercel project, set repo secrets, push the `v0.1.0` tag, post Show HN.
- ✅ **RX2 + M2.5 — Reading Chrome Modes + hover zones** — `chromeMode: 'reading' | 'working'` persistent pref (default `reading`); `zenMode` transient flag stays as the third level via the F key. **Reading mode** hides both sidebars by default; left/right edge 14 px hover zones reveal them as floating drawers with shadow + backdrop; mouseleave starts an 800 ms grace timer before hiding. **Working mode** keeps the previous persistent-sidebar behaviour (`fileTreeOpen` / `tocOpen` prefs). Header gains a `BookOpen ↔ PanelTop` toggle button. (654 tests passing — FileTree.test pinned to `working` so its assumptions hold)
- ✅ **M9.5 — Permission-revoked → ReauthorizeVault** — DocumentPage's load-effect catch block now identifies `VaultPermissionDeniedError` and routes to `missing-vault` state, which already mounts `ReauthorizeVault`. A user who pulls the FSAPI grant mid-session (browser settings → site permissions → revoke) now gets the one-click re-authorize prompt instead of a generic error card.
- ✅ **M7 polish — lightbox / audio / JSON search / copy path** — Image media now opens a Radix Dialog lightbox (Esc / outside-click / close button dismiss; keyboard-accessible via the trigger `<button>`; ZoomIn hint reveals on hover/focus). Audio gains a themed pill wrapper so the native control sits on a tinted surface that matches the rest of the reader. JsonRenderer toolbar grows a search input — case-insensitive substring filter over keys + stringified values; matching paths force-expand their ancestors; matched substrings render inside `<mark>`. Every JSON node grows a hover-revealed Copy button writing a dot/bracket-notation path (`users[0].name`, root → `$`, exotic keys → `["weird-key"]`) to the clipboard. (661 tests passing — +7 polish tests; main bundle 248.49 KB gz, unchanged thanks to MediaRenderer/JsonRenderer being lazy chunks)
- ✅ **M9.4 — First-time hints toast** — Dexie schema v5 adds a `hintsSeen` table; new `useHintsStore` provides `init` / `isSeen` / `markSeen` / `clearAll`. `HintToast` is a generic one-shot component (id + title + children) that reads from the store, auto-dismisses after 12 s (or on close-button click), and writes the seen flag once dismissed. First instance lives in `VaultLayout` introducing `⌘K` / `F` / `?` to first-time vault visitors. Settings panel gains a "Reset hints" button next to "Reset to defaults" so a user can refresh the tour. Toast hidden in zen mode via existing `body.zen-mode` rule. (671 tests passing — +10 hints-store + HintToast tests; main bundle 249.00 KB gz)
- ✅ **Phase 2A — VaultFileSystem.writeText foundation** (commit `ad1b82b`) — `VaultWriteError`; `FSAPIVaultAdapter.writeText` (passes `create: false`, revokes cached blob URL on success, maps `NotAllowedError` → `VaultPermissionDeniedError`); `hasWritePermission` / `requestWritePermission`; `SampleVaultAdapter` rejects `writeText` with a typed `VaultWriteError` carrying a "read-only" reason so the editor surface can route the user to "open my vault".
- ✅ **Phase 2B — Editor session state + dirty navigation guard** (2026-05-03) — `src/stores/editor-store.ts` carries one `EditorSession` (`vaultId` / `path` / `original` / `draft` / `dirty` / `saving` / `error` / `conflict`) with a `save` loop that does the stale-on-disk pre-check + lazy write-permission escalation, plus `overwrite` / `reloadFromDisk` / `cancel` / `clearError` / `forgetVault`. Errors are normalised into a small `EditorError` discriminated union (`permission-denied` / `file-missing` / `write-failed` / `read-only-vault` / `unknown`). `useDirtyNavigationGuard()` mounts a `beforeunload` listener at the AppShell while a session is dirty; `confirmLeaveIfDirty()` is the sync gate for in-app nav. `useVaultStore.removeVault` fan-out drops any session targeting the evicted vault. **732 tests passing** (+29 vs pre-slice).
- ✅ **Phase 2C — DocumentEditSurface (CodeMirror 6 lightweight editor)** (2026-05-03) — Edit button in the document header → `DocumentEditSurface` (lazy chunk) → CodeMirror 6 with markdown syntax + history + search + line wrapping. Toolbar exposes status pill (`saved` / `dirty` / `saving`), Save (primary; disabled when clean), Cancel, and Find. `Mod-S` saves and exits per the lightweight-editing plan UX; `Esc` cancels with a dirty-prompt. Conflict banner offers `Reload from disk` + `Overwrite anyway`; error banners cover all five `EditorError` kinds. Editor mount is once-per-`(vaultId, path)`; reverse back-prop into CodeMirror is guarded against ping-pong with `reloadFromDisk`. **743 tests passing** (+11 EditSurface tests). Main bundle **256.91 KB gz** (Δ +0.29 KB — purely the lazy-import wrapper). New `DocumentEditSurface-*.js` chunk **181.65 KB gz** loads only on first Edit click. Six `@codemirror/*` 6.x packages added (no umbrella).
- ✅ **Phase 2D — Editor polish: isReadOnly gate + Radix confirm + useBlocker + editor prefs** (2026-05-03) — sync `VaultFileSystem.isReadOnly: boolean` pre-flights the Edit button (SampleVault never shows it). New `dialog-store` exposes `requestConfirmation(opts) → Promise<boolean>`; `ConfirmDialog` (lazy Radix) renders the prompt with auto-focused Cancel + danger-styled confirm. `useDirtyNavigationGuard.confirmLeaveIfDirty` and `DocumentEditSurface.handleCancel` both promoted from `window.confirm` to the Radix flow. `useRouterDirtyBlocker()` mounts React Router 7 `useBlocker` so in-app navigation prompts before discarding a dirty session. Three persisted UI prefs (`editorLineNumbers`, `editorLineWrap`, `editorFontSize`) added; CodeMirror Compartments swap extensions live without losing undo history. SettingsPanel gains an "Editing" group. **747 tests passing** (+4 dialog-store tests). Main bundle **257.75 KB gz** (Δ +0.84 KB), EditSurface chunk **183.85 KB gz** (Δ +2.2 KB), new `ConfirmDialog-*.js` chunk **0.56 KB gz** (Radix runtime shared with SettingsPanel/ShortcutsHelp). **Phase 2 lightweight editing is feature-complete** per `docs/develop/lightweight-editing-plan.md`.
- ✅ **Phase 3 first cut — ⌘K `?` mode, end-to-end** (2026-05-07) — pure-fetch `AIProvider` interface (`src/core/ai/types.ts`) + Anthropic adapter (Claude Messages + the `anthropic-dangerous-direct-browser-access` opt-in) + OpenAI-compatible adapter (one shape covers OpenAI / DeepSeek / Together / Ollama / LM Studio; empty Bearer for local LMs). Shared SSE line-event reader. `AIError` discriminated by `kind` so the UI maps `auth` / `rate-limited` / `network` / `aborted` / `malformed-response` / `unknown` to one-line human labels. `key-store` writes API keys AES-GCM-encrypted at rest under a non-extractable master `CryptoKey` stored in the existing `preferences` table; per-provider `aiKeys` table (Dexie v7) carries `{ ciphertext, iv, meta }`. SettingsPanel "AI assistant" group with provider segmented control + Save / Test connection / Clear key + saved badge — plaintext key never round-trips through the React tree after a successful save. Command palette gains a `?` prefix mode that streams an answer below the input via `PaletteAskResult`; ask mode hides the static groups so the answer owns the surface. Context = current document + 1-hop wikilink neighbours (cap 4 files / 8k chars each / 30k chars total; non-Markdown skipped; self-skip; truncation marker). Footer hint mentions both `>` and `?`. **831 tests passing** (+38 across `ai/sse`, `ai/anthropic-provider`, `ai/openai-compatible-provider`, `ai/key-store`, `wikilink-extractor`, `AIControl`, palette ask integration). Main bundle **259.95 KB gz** (Δ +0.07 vs Phase 2D); CommandPalette lazy chunk **9.05 KB gz**; SettingsPanel lazy chunk **5.40 KB gz**.
- 🔜 Pick next: 3F real-key smoke test against a live Anthropic / DeepSeek / Ollama backend, then iterate on Phase 3 polish (provider preference picker, prompt cache, embedding-based recall) per `docs/design/ai-roadmap.md`.

## Before Coding

Read the design docs in `docs/design/` first. The product is fully designed — your job is implementation, not redesign.

## Phase 1 Milestones at a Glance

| Milestone | Goal                                                                     |
| --------- | ------------------------------------------------------------------------ |
| **M0**    | Project bootstrap (Vite, Tailwind, fonts, routing)                       |
| **M1**    | First real render: open vault, see one note in Sepia ⭐ key milestone    |
| **M2**    | Reading shell + 5 themes + zen mode                                      |
| **M3**    | Markdown completeness (wikilinks, callouts, embeds, math, code, mermaid) |
| **M4**    | Navigation (file tree, backlinks, TOC, recent files)                     |
| **M5**    | ⌘K command palette with multiple modes                                   |
| **M6**    | Multi-vault support and returning user flow                              |
| **M7**    | Universal file reader (txt, csv, json, html, code, media)                |
| **M8**    | Landing page + sample vault                                              |
| **M9**    | Polish, perf, mobile, ship                                               |

**M1 is the proof-of-concept moment.** Once a real knowledge note from Wilson's vault renders beautifully, the project is real.
